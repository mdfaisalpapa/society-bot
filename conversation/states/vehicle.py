from enum import Enum, auto


class VehicleState(Enum):

    TYPE = auto()

    NUMBER = auto()

    MAKE = auto()

    MODEL = auto()

    COLOR = auto()

    CONFIRM = auto()

    COMPLETE = auto()
